services: 
  php-escort-lxh:
    build:
      context: .
      dockerfile: dockerfile
    volumes:
      - ../:/workspace:cached
    networks:
      - escort-lxh
    ports:
      - "0:8000"
  mysql:
    image: mysql:8
    volumes:
      - mysql-escort-lxh:/var/lib/mysql
    networks:
      - escort-lxh
    ports:
      - "0:3306"    
    environment:
      MYSQL_ROOT_PASSWORD: oot
      MYSQL_USER: oder
      MYSQL_PASSWORD: oder
      
networks:
  escort-lxh:
    driver: bridge

volumes:
  mysql-escort-lxh:
